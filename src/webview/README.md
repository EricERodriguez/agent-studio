# Webview Host Notes

This folder is reserved for extension-host webview helpers and bridge utilities.

Current implementation lives in:
- `src/views/dashboardPanel.ts` (panel host + message bridge)
- `webview/app/*` (React application)
